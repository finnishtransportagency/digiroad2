{
  "name": "asset-history-processor",
  "version": "1.0.0",
  "description": "Lambda for saving asset changes",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive --require mocha-suppress-logs",
    "compile": "tsc",
    "watch": "tsc -w --preserveWatchOutput",
    "post-compile": "rm .dockerignore Dockerfile README.md tsconfig.json && rm -r src test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.245.0",
    "@tsconfig/node18": "^1.0.1",
    "@types/aws-lambda": "^8.10.109",
    "@types/node": "^18.11.10",
    "@types/pg": "^8.6.6",
    "chai": "^4.3.7",
    "mocha": "^10.1.0",
    "mocha-suppress-logs": "^0.3.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "pg": "^8.8.0"
  }
}
