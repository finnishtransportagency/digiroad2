Testiin / tuotantoonsiirto omalta koneelta:

Ssh avain pitää olla koneen web käyttäjän authorized keys listalla, jotta pystyt loggaamaan sisään ilman salasanaa.
Ssh konfit omalla koneella:

Host gateway
	ProxyCommand ssh reaktor nc -q0 80.248.164.107 22

Host education
    User ktunnus
    ProxyCommand ssh gateway nc 10.129.47.165 22

Host production1
    User ktunnus
    ProxyCommand ssh gateway nc 10.129.64.203 22

Host production2
    User ktunnus
    ProxyCommand ssh gateway nc 10.129.64.204 22


- Asenna ruby
- Asenna bundler (apt-get install, yum install, brew install... )
- Asenna capistrano: gem install capistrano
- Releasen julkaisu, digiroad2 juuressa:
* bundle exec cap staging deploy
* bundle exec cap production deploy


1.

Tiedoston lisääminen:
ssh-add -L (tai cat .ssh/id_*.pub)

Logataksesi web käyttäjänä kopioi ssh-add -L antama avain web-käyttäjän:  ~/.ssh/authorized_keys
(echo "ssh-rsa AAAAB...GLUpnJkew== /Users/ramik/.ssh/id_rsa" >> .ssh/authorized_keys)

2.

Pannujen valmistelu:
- kopioi jenkinsin (livi ci:ltä) id_rsa (web-käyttäjä:  ~/.ssh/)
- mikäli portti 22 on suljettu: aseta github .ssh/configissa näin:

Host github.com
  Hostname ssh.github.com
  Port 443

- asenna Java (sudo yum install java-1.7.0-openjdk.x86_64)
- asenna Git (sudo yum install git.x86_64)
- asenna Node (https://github.com/joyent/node/wiki/Installing-Node.js-via-package-manager#rhelcentosscientific-linux-6)
- asenna Bower (npm install -g bower)
- asenna Grunt (npm install -g grunt-cli)


(bower "fix": git config --global url."https://".insteadOf git://)

update git (http://www.halyph.com/2013/08/how-to-install-git-1834-on-centos-64.html)

Oracle libit pitää olla kopioituna web-käyttäjän ~/digiroad2/oracle/ alle

New Relic pitää olla kopioituna: ~/digiroad/newrelic hakemistoon

bonecp properties pitää olla ~/digiroad/ hakemistossa